# ใช้ตอนรันแอปบนเครื่องตัวเอง (ไม่ใช้ Docker)
# คัดลอกเป็น .env แล้วแก้ค่าให้ตรงกับ MySQL ของคุณ
# ถ้ารันด้วย Docker (compose) ไม่ต้องใช้ไฟล์นี้ — ค่า env มาจาก compose อยู่แล้ว

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=fixmyroom
